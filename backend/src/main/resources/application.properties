spring.application.name=SistemaVentasRutasBackend

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/ventas_rutas}
spring.datasource.username=${DB_USER:ventas_app}
spring.datasource.password=${DB_PASSWORD:ventas_dev}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

spring.flyway.enabled=true
